language: c
notifications:
  email:
    - paul4hough@gmail.com

install:
  - sudo apt-get -y update

before_script:
  - type python
  - python --version
  - type bundle
  - env

script:
  - python setup.py test_unit
  - sudo -l
  - sudo sudo -u $USER bash -l env
  - sudo -E env
  - sudo -E python setup.py test_system

after_script:
  - puppet --version
  - cat ~/.ssh/config
  - find . -type f -ls
